# Required
version: 2

build:
    os: ubuntu-22.04
    tools:
        python: "3.9"

sphinx:
    builder: html
    configuration: docs/conf.py
    fail_on_warning: true

python:
    install:
        - requirements: dev-requirements.txt
        - method: pip
          path: .
